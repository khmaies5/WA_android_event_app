<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/eventFragment">

    <fragment
        android:id="@+id/eventFragment"
        android:name="com.khmaies.waandroideventapp.presentation.events.EventFragment"
        android:label="Events"
        tools:layout="@layout/fragment_event" />
    <fragment
        android:id="@+id/meetingFragment"
        android:name="com.khmaies.waandroideventapp.presentation.meetings.MeetingFragment"
        android:label="Meetings"
        tools:layout="@layout/fragment_meeting" />
    <fragment
        android:id="@+id/taskFragment"
        android:name="com.khmaies.waandroideventapp.presentation.tasks.TaskFragment"
        android:label="Tasks"
        tools:layout="@layout/fragment_task" />
    <fragment
        android:id="@+id/userFragment"
        android:name="com.khmaies.waandroideventapp.presentation.users.UserFragment"
        android:label="Users"
        tools:layout="@layout/fragment_user" />
</navigation>